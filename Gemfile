source "https://rubygems.org"

# Use specific version if desired, otherwise uses system/mise ruby
# ruby "3.4.7"

gem "github-pages", group: :jekyll_plugins

# Required for Ruby 3.0+ as it was removed from stdlib
gem "webrick"
